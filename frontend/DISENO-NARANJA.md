# Diseño naranja (pendiente en el repo)

En Git **no hay** todavía la landing con acento **naranja** que describís.

Lo que sí está subido por el equipo (rama `develop`, Scarlet):

- shadcn + Hero en React
- Colores **azul marino** (`#031635`) y **verde/teal** (`#2D7A78` en `globals.css`)

Lo que **no** es el diseño nuevo:

- `public/prototipo/prototipo.html` → Material **verde** `#156967` (prototipo viejo de Ricardo)
- La landing que maquetaste vos antes (eliminada de `main`)

## Para implementar el naranja bien

Pedile a tu compañera **uno** de estos y lo integramos en `main`:

1. Link de **Figma** (o export de variables de color)
2. **Rama** o PR con su código más reciente
3. Captura + hex del naranja (ej. `#F97316`, `#FF6B35`)

Cuando tengamos eso, actualizamos `globals.css`, `button`/`badge` y secciones para que coincida con su diseño.
